import React from "react";
import styles from './main.module.css';

const UnitedSubHeaderButton = ({items}) => {
    return (
      <div className={styles.button}>
        {/* <img src={img} />
        <span>{title}</span> */}
      </div>
    )
};

export default UnitedSubHeaderButton;